name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: generate
      run: cmake . -A x64 -B .build
    - name: build
      run: cmake --build .build
